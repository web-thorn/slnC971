<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WGU.Views.TermsPage"
             xmlns:vm="clr-namespace:WGU.ViewModels"
             xmlns:local="clr-namespace:WGU.ViewModels"
             xmlns:model="clr-namespace:WGU.Models"
             Title="{Binding Title}">
    <StackLayout>
        <Label Text="My WGU Terms"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        <RefreshView x:DataType="local:TermsViewModel" Command="{Binding LoadTermsCommand}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
            <CollectionView x:Name="TermsListView"
                                ItemsSource="{Binding Terms}"
                                SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10" x:DataType="model:Term">
                            <Label Text="{Binding Name}"
                                       LineBreakMode="NoWrap"
                                       Style="{DynamicResource ListItemTextStyle}"
                                       FontSize="16" />
                            <Label Text="{Binding TermDate}"
                                       LineBreakMode="NoWrap"
                                       Style="{DynamicResource ListItemTextStyle}"
                                       FontSize="16" />
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer
                                    
                                    >
                                    
                                </TapGestureRecognizer>
                            </StackLayout.GestureRecognizers>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </StackLayout>
</ContentPage>